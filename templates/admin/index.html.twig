{% extends 'base_admin.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Bienvenue sur l'interface d'administration</h1>
    <table>
        <thead>
            <tr>
                <th>
                    Nom d'utilisateur
                </th>
                <th>
                    Email
                </th>
                <th>
                    Actions
                </th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
        <tr>
            <td>
                {{ user.username }}
            </td>
            <td>
                {{ user.email }}
            </td>
            <td>
                <a href="{{ path('admin_user_details', { id: user.id }) }}">Voir les détails</a>
            </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
